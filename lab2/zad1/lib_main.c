//
// Created by lukasz on 09.03.23.
//
#include <stdio.h>

int main(int idk, char** args)
{
    char a, b;
    char filename1[256];
    char filename2[256];

    printf(args);

    //if(sscanf(args,"%c %c %s %s\n", &a,&b, filename1, filename2) == 4)
    //{
    //    printf("%c %c %s %s", a,b,filename1, filename2);
    //}

}